# 光影手记（Shimmer）项目分析与系统设计文档

---

## 1 项目概述

### 1.1 项目主题

**光影手记（Shimmer）** 是一个精美的个人照片日记 Web 应用，旨在帮助用户用照片记录生活中的每一个光影瞬间。

### 1.2 核心目标

- 为用户提供一个美观、沉浸式的照片浏览体验
- 支持照片的高效上传、自动压缩与存储管理
- 允许管理员（照片上传者）便捷地管理个人照片日记
- 为访客提供照片浏览与收藏功能，记录美好瞬间

### 1.3 核心业务流程

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   管理员登录    │ ──▶ │  上传照片并编辑  │ ──▶ │  发布照片公开   │
└─────────────────┘     └─────────────────┘     └─────────────────┘
                                                          │
                                                          ▼
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│    访客浏览     │ ◀── │  瀑布流/时间线   │ ◀── │   照片存储      │
└─────────────────┘     └─────────────────┘     └─────────────────┘
         │
         ▼
┌─────────────────┐
│   收藏照片      │
└─────────────────┘
```

### 1.4 主要参与者

| 角色 | 描述 | 权限 |
|------|------|------|
| 管理员 | 照片上传者和管理者 | 登录、增删改照片、管理个人账户 |
| 访客 | 照片浏览者 | 浏览、查看详情、收藏照片 |

---

## 2 需求分析

### 2.1 功能需求

#### 2.1.1 用户认证模块

| 功能 | 描述 |
|------|------|
| 用户登录 | 管理员使用用户名密码登录系统 |
| 用户登出 | 清除登录状态 |
| 获取当前用户 | 获取已登录用户信息 |
| 修改密码 | 管理员修改登录密码 |

#### 2.1.2 照片管理模块

| 功能 | 描述 |
|------|------|
| 照片列表 | 分页获取公开/所有照片列表 |
| 照片详情 | 获取单张照片详细信息 |
| 上传照片 | 上传新照片，自动压缩生成缩略图 |
| 编辑照片 | 修改照片标题、心情文字、拍摄日期、地点等信息 |
| 删除照片 | 删除指定照片及相关资源 |
| 随机日记 | 获取随机一条心情日记 |
| 时间线统计 | 获取按月份/年份统计的照片数量 |
| 地图标记 | 获取带有GPS信息的照片位置标记 |

#### 2.1.3 照片展示模块

| 功能 | 描述 |
|------|------|
| 瀑布流展示 | 网格布局展示照片，图片高度自适应 |
| 时间线视图 | 按拍摄日期时间顺序展示照片 |
| 暗房模式 | 暗色背景沉浸式浏览照片 |
| 照片详情页 | 查看照片详细信息和大图 |

#### 2.1.4 收藏模块

| 功能 | 描述 |
|------|------|
| 查看收藏 | 获取当前访客的收藏列表 |
| 添加收藏 | 收藏喜欢的照片 |
| 取消收藏 | 取消已收藏的照片 |
| 检查收藏状态 | 检查指定照片是否已被收藏 |

#### 2.1.5 年度回顾模块

| 功能 | 描述 |
|------|------|
| 年度回顾 | 按年份查看年度照片回顾 |
| 可用年份 | 获取有照片数据的年份列表 |

### 2.2 非功能性需求

| 需求类型 | 描述 |
|----------|------|
| 性能 | 照片列表加载时间 < 2秒，图片懒加载 |
| 可用性 | 响应式设计，适配桌面端和移动端 |
| 安全性 | 密码使用bcrypt加密，API使用JWT认证 |
| 可维护性 | 前后端分离，模块化设计 |
| 数据完整性 | 外键约束确保数据一致性 |

---

## 3 系统设计

### 3.1 UML建模

#### 3.1.1 用例图

![plantuml (5)](C:\Users\m1323\Downloads\plantuml (5).png)



#### 3.1.2 核心活动图——用户登录并上传照片

![plantuml (2)](C:\Users\m1323\Downloads\plantuml (2).png)



#### 3.1.3 核心时序图——前端请求照片列表并渲染

![plantuml (3)](C:\Users\m1323\Downloads\plantuml (3).png)



### 3.2 数据库设计

#### 3.2.1 概念模型（E-R图）

![plantuml (4)](C:\Users\m1323\Downloads\plantuml (4).png)



#### 3.2.2 逻辑模型

| 表名 | 字段名 | 数据类型 | 说明 |
|------|--------|----------|------|
| **users** | id | BIGINT UNSIGNED | 用户ID（主键） |
| | username | VARCHAR(50) | 用户名（唯一） |
| | password_hash | VARCHAR(255) | bcrypt加密密码 |
| | avatar | VARCHAR(500) | 头像URL |
| | created_at | DATETIME | 创建时间 |
| | updated_at | DATETIME | 更新时间 |
| **photos** | id | BIGINT UNSIGNED | 照片ID（主键） |
| | user_id | BIGINT UNSIGNED | 上传者ID（外键） |
| | title | VARCHAR(200) | 照片标题 |
| | url | VARCHAR(500) | 压缩后图片URL |
| | thumbnail_url | VARCHAR(500) | 缩略图URL |
| | original_url | VARCHAR(500) | 原始图片URL |
| | mood | TEXT | 心情文字 |
| | shot_date | DATE | 拍摄日期 |
| | location | VARCHAR(200) | 拍摄地点 |
| | width | INT UNSIGNED | 图片宽度 |
| | height | INT UNSIGNED | 图片高度 |
| | file_size | INT UNSIGNED | 文件大小(KB) |
| | sort_order | INT | 排序权重 |
| | is_visible | TINYINT(1) | 是否公开 |
| | created_at | DATETIME | 创建时间 |
| | updated_at | DATETIME | 更新时间 |
| **favorites** | id | BIGINT UNSIGNED | 收藏ID（主键） |
| | photo_id | BIGINT UNSIGNED | 照片ID（外键） |
| | fingerprint | VARCHAR(64) | 访客浏览器指纹 |
| | created_at | DATETIME | 收藏时间 |

#### 3.2.3 物理模型（MySQL建表SQL）

```sql
-- ============================================
-- 光影手记 / SNAPSHOT — 数据库建表脚本
-- 数据库：MySQL 8.x
-- 字符集：utf8mb4（支持完整 Unicode，含 emoji）
-- ============================================

-- 创建数据库
CREATE DATABASE IF NOT EXISTS shimmer
  DEFAULT CHARACTER SET utf8mb4
  DEFAULT COLLATE utf8mb4_unicode_ci;

USE shimmer;

-- --------------------------------------------
-- 1. 用户表（管理员账户）
-- --------------------------------------------
DROP TABLE IF EXISTS `photos`;
DROP TABLE IF EXISTS `favorites`;
DROP TABLE IF EXISTS `users`;

CREATE TABLE `users` (
  `id`          BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '用户 ID',
  `username`    VARCHAR(50)     NOT NULL COMMENT '用户名（唯一，用于登录）',
  `password_hash` VARCHAR(255)  NOT NULL COMMENT '密码哈希（bcrypt）',
  `avatar`      VARCHAR(500)    DEFAULT NULL COMMENT '头像 URL',
  `created_at`  DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at`  DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',

  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='管理员用户表';


-- --------------------------------------------
-- 2. 照片表
-- --------------------------------------------
CREATE TABLE `photos` (
  `id`            BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '照片 ID',
  `user_id`       BIGINT UNSIGNED NOT NULL COMMENT '上传者（管理员）ID',
  `title`         VARCHAR(200)    DEFAULT NULL COMMENT '照片标题',
  `url`           VARCHAR(500)    NOT NULL COMMENT '原图 URL（压缩后 200-500KB）',
  `thumbnail_url` VARCHAR(500)    DEFAULT NULL COMMENT '缩略图 URL（用于列表展示）',
  `original_url`  VARCHAR(500)    DEFAULT NULL COMMENT '原始未压缩图片 URL（可选备份）',
  `mood`          TEXT            DEFAULT NULL COMMENT '心情文字（一句话日记）',
  `shot_date`     DATE            DEFAULT NULL COMMENT '拍摄日期',
  `location`      VARCHAR(200)    DEFAULT NULL COMMENT '拍摄地点',
  `width`         INT UNSIGNED    DEFAULT NULL COMMENT '图片宽度（px，瀑布流布局用）',
  `height`        INT UNSIGNED    DEFAULT NULL COMMENT '图片高度（px，瀑布流布局用）',
  `file_size`     INT UNSIGNED    DEFAULT NULL COMMENT '压缩后文件大小（KB）',
  `sort_order`    INT             DEFAULT 0 COMMENT '手动排序权重，越大越靠前',
  `is_visible`    TINYINT(1)      NOT NULL DEFAULT 1 COMMENT '是否公开：1=公开，0=隐藏',
  `created_at`    DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（上传时间）',
  `updated_at`    DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',

  PRIMARY KEY (`id`),
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_is_visible` (`is_visible`),
  INDEX `idx_sort_order` (`sort_order`),
  INDEX `idx_created_at` (`created_at`),
  INDEX `idx_shot_date` (`shot_date`),
  INDEX `idx_visible_created` (`is_visible`, `created_at`),
  INDEX `idx_visible_sort` (`is_visible`, `sort_order`),

  CONSTRAINT `fk_photos_user_id`
    FOREIGN KEY (`user_id`) REFERENCES `users` (`id`)
    ON DELETE CASCADE ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='照片表';


-- --------------------------------------------
-- 3. 收藏表（访客收藏）
-- --------------------------------------------
CREATE TABLE `favorites` (
  `id`          BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '收藏 ID',
  `photo_id`    BIGINT UNSIGNED NOT NULL COMMENT '照片 ID',
  `fingerprint` VARCHAR(64)     NOT NULL COMMENT '访客浏览器指纹（唯一标识匿名用户）',
  `created_at`  DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '收藏时间',

  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_photo_fingerprint` (`photo_id`, `fingerprint`),
  INDEX `idx_fingerprint_created` (`fingerprint`, `created_at`),

  CONSTRAINT `fk_favorites_photo_id`
    FOREIGN KEY (`photo_id`) REFERENCES `photos` (`id`)
    ON DELETE CASCADE ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='访客收藏表';
```

---

## 4 技术架构图

![plantuml (6)](C:\Users\m1323\Downloads\plantuml (6).png)

### 4.1 技术栈说明

| 层级 | 技术 | 版本 | 用途 |
|------|------|------|------|
| 前端框架 | Vue.js | 3.5.12 | UI框架 |
| 前端路由 | Vue Router | - | 单页应用路由 |
| 状态管理 | Pinia | - | 全局状态管理 |
| HTTP客户端 | Axios | - | API请求 |
| 构建工具 | Vite | - | 开发/构建 |
| 后端框架 | Express.js | 4.21.0 | Web服务器 |
| 数据库 | MySQL | 8.0 | 关系型数据库 |
| 图片处理 | Sharp | - | Node.js图片处理 |
| 密码加密 | Bcrypt | - | 密码哈希 |
| 认证 | JWT | - | Token认证 |
| 文件上传 | Multer | - | Node.js文件上传 |
| 反向代理 | Nginx | - | 生产环境部署 |

### 4.2 组件交互流程

1. **用户访问流程**：浏览器 → Nginx/Vite Proxy → Express API
2. **数据流程**：Express → MySQL数据库 / 文件系统
3. **状态管理**：Pinia Store → 组件响应式更新
4. **认证流程**：JWT Token → Authorization Header → 后端验证

---

## 附录

### A. 默认账户信息

- 用户名：`admin`
- 密码：`admin123`

### B. API 接口列表

| 模块 | 方法 | 路径 | 描述 |
|------|------|------|------|
| 认证 | POST | /api/auth/login | 登录 |
| 认证 | POST | /api/auth/logout | 登出 |
| 认证 | GET | /api/auth/me | 获取当前用户 |
| 照片 | GET | /api/photos | 获取照片列表 |
| 照片 | GET | /api/photos/:id | 获取照片详情 |
| 照片 | POST | /api/photos | 上传照片 |
| 照片 | PUT | /api/photos/:id | 更新照片 |
| 照片 | DELETE | /api/photos/:id | 删除照片 |
| 收藏 | GET | /api/favorites | 获取收藏列表 |
| 收藏 | POST | /api/favorites | 添加收藏 |
| 收藏 | DELETE | /api/favorites/:photoId | 取消收藏 |

---

*文档版本：v1.0*
*创建日期：2026-03-25*
